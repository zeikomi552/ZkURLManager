<Window x:Class="ZkURLManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZkURLManager"
        xmlns:prism="http://prismlibrary.com/"
        mc:Ignorable="d"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="500" Width="900">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="16" Width="760">
            <TextBlock Text="{Binding Title}" FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,12"/>

            <TextBlock Text="URL テンプレート" Margin="0,0,0,4" />
            <TextBox Text="{Binding UrlTemplate, UpdateSourceTrigger=PropertyChanged}" Height="28" Margin="0,0,0,8" />

            <DataGrid ItemsSource="{Binding Entries}"
                      SelectedItem="{Binding SelectedEntry, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      Height="220"
                      Margin="0,0,0,12">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Key" Binding="{Binding Key, Mode=TwoWay}" Width="*" />
                    <DataGridTextColumn Header="Value" Binding="{Binding Value, Mode=TwoWay}" Width="2*" />
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
                <Button Content="追加" Command="{Binding AddEntryCommand}" Width="120" Margin="0,0,8,0" />
                <Button Content="削除" Command="{Binding RemoveEntryCommand}" Width="120" />
            </StackPanel>

            <TextBlock Text="生成された URL" Margin="0,0,0,4" />
            <TextBox Text="{Binding RenderedUrl, Mode=OneWay}" IsReadOnly="True" Height="28" Margin="0,0,0,12" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,0">
                <Button Content="開く" Command="{Binding OpenUrlCommand}" Width="120" Margin="0,0,8,0" />
                <Button Content="保存" Command="{Binding SaveCommand}" Width="120" Margin="0,0,8,0" />
                <Button Content="読み込む" Command="{Binding LoadCommand}" Width="120" Margin="0,0,8,0" />
                <Button Content="パラメータをコピー" Command="{Binding CopyParametersCommand}" Width="160" Margin="0,0,8,0" />
                <Button Content="テンプレートをコピー" Command="{Binding CopyParameterTemplateCommand}" Width="160" Margin="0,0,8,0" />
                <Button Content="終了" Command="{Binding ExitCommand}" Width="120"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
