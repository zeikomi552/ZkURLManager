<mah:MetroWindow x:Class="ZkURLManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZkURLManager"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="640" Width="1200">
    <Grid>
        <Border CornerRadius="8" Padding="16" Margin="16">
            <Border.Effect>
                <DropShadowEffect Color="#22000000" BlurRadius="14" ShadowDepth="4" />
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- プリセットリスト -->
                <StackPanel Grid.Column="0" Margin="8">
                    <TextBlock Text="プリセット" FontWeight="SemiBold" Margin="4" />
                    <ListBox ItemsSource="{Binding Presets}" SelectedItem="{Binding SelectedPreset}" Height="380">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="4">
                                    <materialDesign:PackIcon Kind="{Binding Icon}" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,8,0" />
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding Description}" FontSize="11" Foreground="{DynamicResource MaterialDesignBody}" />
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
                        <Button Command="{Binding AddPresetCommand}" Style="{DynamicResource MaterialDesignRaisedButton}" Width="60">追加</Button>
                        <Button Command="{Binding DuplicatePresetCommand}" Style="{DynamicResource MaterialDesignFlatButton}" Width="70" Margin="6,0,0,0">複製</Button>
                        <Button Command="{Binding RemovePresetCommand}" Style="{DynamicResource MaterialDesignFlatButton}" Width="60" Margin="6,0,0,0">削除</Button>
                        <Button Command="{Binding DataContext.MovePresetUpCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{DynamicResource MaterialDesignFlatButton}" Width="auto" Margin="12,0,0,0">
                            <materialDesign:PackIcon Kind="ArrowUp" Width="16" Height="16" />
                        </Button>
                        <Button Command="{Binding DataContext.MovePresetDownCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{DynamicResource MaterialDesignFlatButton}" Width="auto" Margin="6,0,0,0">
                            <materialDesign:PackIcon Kind="ArrowDown" Width="16" Height="16" />
                        </Button>
                    </StackPanel>
                </StackPanel>

                <!-- プリセット詳細 -->
                <StackPanel Grid.Column="1" Margin="8">
                    <TextBlock Text="プリセット名" Margin="0,0,0,4" />
                    <TextBox Text="{Binding SelectedPreset.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                    <TextBlock Text="説明" Margin="0,0,0,4" />
                    <TextBox Text="{Binding SelectedPreset.Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

                    <TextBlock Text="URL テンプレート" Margin="0,0,0,4" />
                    <TextBox Text="{Binding UrlTemplate, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                    <DataGrid ItemsSource="{Binding Entries}"
                              SelectedItem="{Binding SelectedEntry, Mode=TwoWay}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              Height="220"
                              Margin="0,0,0,12"
                              RowHeaderWidth="0"
                              HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Key" Binding="{Binding Key, Mode=TwoWay}" Width="*" />
                            <DataGridTextColumn Header="Value" Binding="{Binding Value, Mode=TwoWay}" Width="2*" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
                        <Button Command="{Binding AddEntryCommand}" Width="110" Style="{DynamicResource MaterialDesignRaisedButton}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <materialDesign:PackIcon Kind="Plus" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,6,0" />
                                <TextBlock Text="追加" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding RemoveEntryCommand}" Width="110" Style="{DynamicResource MaterialDesignRaisedButton}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <materialDesign:PackIcon Kind="Minus" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,6,0" />
                                <TextBlock Text="削除" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding DataContext.MoveEntryUpCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{DynamicResource MaterialDesignFlatButton}" Width="auto" Margin="8,0,0,0">
                            <materialDesign:PackIcon Kind="ArrowUp" Width="16" Height="16" />
                        </Button>
                        <Button Command="{Binding DataContext.MoveEntryDownCommand, RelativeSource={RelativeSource AncestorType=Window}}" Style="{DynamicResource MaterialDesignFlatButton}" Width="auto" Margin="6,0,0,0">
                            <materialDesign:PackIcon Kind="ArrowDown" Width="16" Height="16" />
                        </Button>
                    </StackPanel>

                    <TextBlock Text="生成された URL" Margin="0,0,0,4" />
                    <TextBox Text="{Binding RenderedUrl, Mode=OneWay}" IsReadOnly="True"  Margin="0,0,0,12"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,0">
                        <Button Command="{Binding OpenUrlCommand}" Width="140" Style="{DynamicResource MaterialDesignFlatButton}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <materialDesign:PackIcon Kind="OpenInNew" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,6,0" />
                                <TextBlock Text="開く" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding SaveCommand}" Width="120" Style="{DynamicResource MaterialDesignRaisedButton}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ContentSave" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,6,0" />
                                <TextBlock Text="保存" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding LoadCommand}" Width="120" Style="{DynamicResource MaterialDesignRaisedButton}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FolderOpen" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,6,0" />
                                <TextBlock Text="読み込む" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding CopyParametersCommand}" Width="180" Style="{DynamicResource MaterialDesignOutlinedButton}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ContentCopy" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,6,0" />
                                <TextBlock Text="パラメータをコピー" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding CopyParameterTemplateCommand}" Width="180" Style="{DynamicResource MaterialDesignOutlinedButton}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ContentCopy" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,6,0" />
                                <TextBlock Text="テンプレートをコピー" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding ExitCommand}" Width="120" Style="{DynamicResource MaterialDesignFlatButton}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ExitToApp" Width="18" Height="18" VerticalAlignment="Center" Margin="0,0,6,0" />
                                <TextBlock Text="終了" VerticalAlignment="Center" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</mah:MetroWindow>
